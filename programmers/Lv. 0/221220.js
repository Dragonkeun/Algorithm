// A로 B 만들기
function solution(before, after) {
    before = before.split("")
    for(let i=0; i<before.length; i++){
        if (after.indexOf(before[i])){
            var result = 1
        }
    }
    return result;
}